// use std::process::Command;

fn main() {
    println!("Hello, world!");
    // let mut cmd = Command::new("ls");
    // let res = cmd.output();
    // assert!(res.is_ok());
    // println!("passed!");
    
    
}
